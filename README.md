# 📡 ChebulUP (CUP) — надёжная передача данных через акустический канал

**CUP (Chebul Acoustic Protocol)** — учебно-исследовательский проект по построению
**надёжной передачи цифровых данных через акустический канал**
(динамик → микрофон) в условиях шума и нестабильности.

В качестве физического уровня используется библиотека **ggwave**.
Поверх неё реализуется собственный протокол: кадрирование, контроль целостности,
фрагментация сообщений и надёжная доставка с подтверждениями и повторными передачами.

Проект **не оптимизируется под максимальную скорость**.
Цель — **гарантированная и воспроизводимая доставка небольших объёмов данных**
в принципиально ненадёжном канале.

---

## 1. Мотивация

Акустический канал обладает фундаментальными ограничениями:

* высокий уровень фонового шума;
* искажения динамиков и микрофонов;
* автоматическая обработка аудио со стороны ОС (AGC, шумоподавление);
* потери и искажения сигналов.

Подход вида *«просто передать байты»* в таких условиях не работает.
Требуется **многоуровневый протокол**, аналогичный сетевым (Link / Transport),
но адаптированный под звук.

---

## 2. Физический уровень (PHY)

В качестве PHY используется **ggwave**:

* модуляция: **FSK**;
* синхронизация и framing — внутри ggwave;
* коррекция ошибок: **Reed–Solomon** (встроенная);
* режимы: audible / ultrasonic.

В рамках проекта **DSP не переписывается**.
`ggwave` рассматривается как **чёрный ящик PHY**, поверх которого строится протокол.

### Ограничение Python-обёртки ggwave

* `ggwave.encode()` принимает **строку**, поэтому бинарные кадры
  передаются как **base64**.
* Это увеличивает накладные расходы, но делает baseline стабильным и воспроизводимым.

---

## 3. Архитектура (логическая)

```
Application bytes
        ↓
Message Fragmentation
        ↓
Frame (header + CRC32)
        ↓
Stop-and-Wait ARQ (ACK / timeout / retry)
        ↓
Base64 wrapper (bytes → str)
        ↓
ggwave PHY (FSK + RS)
```

---

## 4. Реализованные уровни

### 4.1 PHY baseline

* Подтверждено детерминированное поведение:

  ```
  encode("abc") → decode → "abc"
  ```
* Используется `protocolId = 0`.

---

### 4.2 Кадры и контроль целостности

* Фиксированный заголовок кадра.
* **CRC32** для обнаружения битых кадров.
* Кадры с ошибкой CRC не принимаются и не подтверждаются.

---

### 4.3 Фрагментация и сборка сообщений

* Сообщение разбивается на несколько DATA-кадров.
* Кадры собираются по `(msg_id, seq, total)`.
* Поддерживается корректная сборка при потере и повторе кадров.

---

### 4.4 Надёжная доставка (Transport)

* Реализован **Stop-and-Wait ARQ**:

  * DATA → ACK;
  * таймаут ожидания;
  * повторная передача при потере;
  * лимит числа попыток.
* ACK подтверждает конкретный кадр.
* Подтверждена корректная работа при потерях **DATA и ACK**.

---

## 5. Структура репозитория

```
CUP/
├── README.md
├── .gitignore
└── scripts/
    ├── __init__.py
    ├── baseline_ggwave_file.py     # проверка PHY (encode → decode)
    ├── ggwave_codec.py             # конвертация PCM / float32
    ├── packet.py                   # frame header + CRC + fragmentation
    ├── test_packet_over_phy.py     # один кадр поверх PHY
    ├── test_fragment_over_phy.py   # фрагментация + сборка
    ├── arq_stop_and_wait.py        # stop-and-wait ARQ поверх PHY
    ├── measure_arq.py              # реальные измерения (ggwave)
    ├── measure_arq_fast_sim.py     # быстрый симулятор без DSP
    └── measure_arq_ggwave_smoke.py # smoke-тест ARQ на ggwave
```

---

## 6. Быстрый старт

### Требования

* Python 3.x
* Виртуальное окружение (`venv`)
* Установленный `ggwave` (совместимые wheels)

Проверка установки:

```bash
python -c "import ggwave; print('ggwave ok')"
```

---

## 7. Проверка корректности

### 7.1 PHY baseline

```bash
python -m scripts.baseline_ggwave_file
```

Ожидается:

```
SUCCESS: protocolId=0
```

---

### 7.2 Кадр поверх PHY

```bash
python -m scripts.test_packet_over_phy
```

---

### 7.3 Фрагментация и сборка

```bash
python -m scripts.test_fragment_over_phy
```

Ожидается:

```
OK
```

---

### 7.4 Stop-and-Wait ARQ

```bash
python -m scripts.arq_stop_and_wait
```

Ожидается:

```
FINAL: OK
```

---

## 8. Измерения производительности

### 8.1 Реальные измерения (ggwave)

```bash
python -m scripts.measure_arq
```

Пример baseline-результата
(`drop DATA = 25%`, `drop ACK = 10%`, payload = 130 байт):

* лучший goodput при 100% успехе:

  * `max_payload = 32`, `timeout = 0.2` → **~50 B/s**
* увеличение timeout повышает устойчивость, но снижает скорость.

> Эти цифры отражают **стоимость протокола + DSP ggwave**,
> а не «чистую пропускную способность канала».

---

### 8.2 Быстрый симулятор (без DSP)

```bash
python -m scripts.measure_arq_fast_sim
```

* Позволяет прогонять **сотни конфигураций** за секунды.
* Используется для подбора `max_payload` и `timeout`
  без долгих ggwave-запусков.

---

### 8.3 Smoke-тест на реальном ggwave

```bash
python -m scripts.measure_arq_ggwave_smoke
```

* Проверяет, что параметры, подобранные в fast-sim,
  **работают и на реальном PHY**.

---

## 9. Ограничения текущей модели

* Ненадёжность моделируется как **потеря кадров**.
* Битовые ошибки (corrupt → CRC-fail) пока не моделируются.
* Реальный микрофон/динамик пока не подключён
  (используется синтетический PHY).

---

## 10. Roadmap

Ближайшие шаги:

1. Добавление **corrupt / CRC-fail** в ggwave-раннер.
2. Вынос протокольной логики в пакет `cup/`.
3. Подключение реального акустического канала:

   * TX: воспроизведение;
   * RX: запись + потоковый decode.
4. (Опционально) Sliding window / selective repeat.

---

## 11. Статус проекта

Проект находится в состоянии **рабочего baseline**:

* протокол реализован;
* измерения воспроизводимы;
* архитектура зафиксирована;
* есть fast-sim и smoke-тесты.

Все дальнейшие улучшения будут делаться **поверх этого baseline**.

---

## 12. Лицензия

Учебный проект.
Лицензия может быть добавлена позже при необходимости.

